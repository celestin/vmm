

Keywords:
		Search keywords: 	create_by_copy	vmm1.2

Technical content:
 
		Title: 		param class  
 
		Objective:     Usage of param class in factory replacement.

		Description:  blueprint and factory are param classes.Used create_by_new to replace the factory 


		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VMM_HOME/examples/testbench/sv/vmm/std_lib/factory_pattern/basic_param_class
		DIAGRAMS: 		None
		Cross Reference:	$VMM_HOME/doc/vmm_user_guide.pdf

